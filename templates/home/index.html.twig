<h1>Users</h1>
<ul>
    {% for user in users %}
        <li><a href="{{ path('user_show', {'id': user.id}) }}">{{ user.email }}</a></li>
    {% endfor %}
</ul>

<a href="{{ path('user_new') }}">Create new user</a>
